import React from "react";

const Header = () => {
    return (
        <header className="min-h-[10vh] flex items-center justify-center bg-slate-600 text-slate-300 text-4xl">
            <h2>Validation test page</h2>
        </header>
    );
};

export default Header;
